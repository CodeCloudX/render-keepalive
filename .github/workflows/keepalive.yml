name: Keep Render Awake

on:
  schedule:
    - cron: "*/13 * * * *"  
  workflow_dispatch:          

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          echo "Pinging Render app..."
          curl -s https://pdfmaster-t8w6.onrender.com > /dev/null
          echo "Ping complete!"